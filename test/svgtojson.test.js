const svgToJson = require('../SvgToJson');


/**
 * Converter class tests.
 */
describe('SvgToJson', () => {

    // beforeAll(() => {
    //
    //     /* Runs before all tests */
    //
    //     console.log('Run beforeAll()')
    // })
    //
    // afterAll(() => {
    //
    //     /* Runs after all tests */
    //
    //     console.log('Run afterAll()')
    // })
    //
    // beforeEach(() => {
    //
    //     /* Runs before each test */
    //
    //     console.log('Run beforeEach()')
    // })
    //
    // afterEach(() => {
    //
    //     /* Runs after each test */
    //
    //     console.log('Run afterEach()')
    // })

    test('SvgToJson.getElements(nodeName) should get all elements match `nodeName`', () => {

        // 1. ARRANGE

        let a = "foo",
            b = "foo";

        // 2. ACT

        // 3. ASSERT
        expect(a).toEqual(b);

    });
});
